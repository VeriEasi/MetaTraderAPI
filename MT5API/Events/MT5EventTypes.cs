namespace MT5API.Events
{
    internal enum MT5EventTypes
    {
        OnTradeTransaction = 1,
        OnBookEvent        = 2,
        OnTick             = 3,
        OnLastTimeBar      = 4,
        OnLockTicks        = 5
    }
}
